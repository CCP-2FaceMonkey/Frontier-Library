/** Library item: id, title, categoryId, date, author, cover + file to open on favicon click */
export interface LibraryItem {
  id: string;
  title: string;
  categoryId: string;
  date: string;
  author: string;
  coverUrl: string;
  fileUrl: string;
}
